# Phase 0 审核记录

**日期:** 2026-07-29
**审核方式:** 外部独立文档审核

---

## 审核对象

- `docs/refactor/phase-0-audit.md` — 项目现状审计
- `docs/refactor/handoffs/phase-0-handoff.md` — Phase 0 交接文档
- Phase 0 运行基线和最终修正结果

---

## 最终结论

**通过**

---

## 阻断问题

无

---

## 已完成的主要修正

| # | 修正内容 |
|---|---------|
| 1 | 补充关键结论证据索引（18 个 Finding，F-001~F-018，含文件定位、关键证据、影响、验证方式） |
| 2 | 补充模块依赖与调用关系图（页面→API Route→lib/features→外部依赖，含细粒度函数调用链） |
| 3 | 补充环境变量和外部依赖清单（3 个 env var、5 个外部服务、4 个本地依赖、2 个文件系统依赖） |
| 4 | 补充 TypeScript、ESLint、Build 静态验证基线 |
| 5 | 补充 Dev Server 只读运行验证（所有静态页面和只读 API 返回 200） |
| 6 | 补充 Neon 数据库实际连通性验证（`/api/warmup` → `{"status":"ok"}`） |
| 7 | 修正冻结范围描述（区分 Coach 严格冻结、Words UI 冻结、schema 最严格冻结、Reading 可受控重构） |
| 8 | 修正 `$queryRawUnsafe` 风险定性（"SQL 注入漏洞" → "安全与维护风险"，说明当前值参数通过占位符传递） |
| 9 | 修正 SM-2 历史记录表述（"标准 SM-2 应记录复习历史" → "当前 WordReview 足以保存当前调度状态，但不保存逐次事件历史"） |
| 10 | 修正 AI 调用文件数量（9 个→10 个独立文件） |
| 11 | 清理已完成但仍被标记为未验证的项目（Dev Server 启动、数据库连接） |

---

## 已知非阻断事项

以下问题已记录在审计文档中，不影响 Phase 1 启动，需在后续阶段逐步处理：

| # | 事项 | 说明 |
|---|------|------|
| 1 | ESLint 当前有 39 个已记录错误和 38 个警告 | 主要为 React 19 新 lint 规则触发，不影响运行 |
| 2 | `public/listening` 构建扫描警告 | 运行时写入 `public/` 目录，Turbopack 扫描 25768 个文件 |
| 3 | DeepSeek 实际调用尚未验证 | 需消耗 API 额度进行端到端验证 |
| 4 | MP3 音频完整性尚未逐个验证 | 5709 个文件依赖 Edge TTS 生成质量 |
| 5 | SM-2 边界情况尚无自动化测试 | 需在 Phase 2（建立评估基线）或 Phase 8 中覆盖 |

---

## 放行结论

审核通过。允许进入 **Phase 1 — 设计目标架构**。

Phase 1 仅限目标架构设计（产出文档），不得包含：
- 实际代码重构
- 安装新依赖
- 修改 schema
- 创建 Service 类
- 统一 AI Client 的实现
